## 0.0.1

* Initial release of `MediaFormField` (formerly `SmartUploadField`).
* Added support for multiple file selection with `minItems` and `maxItems` constraints.
* Integrated `image_picker` and `file_picker` for bulk processing.
* Modernized UI with `withValues` for better color precision.
